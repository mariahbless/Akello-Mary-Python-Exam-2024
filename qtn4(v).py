# Question 


class Car:     # defining the class of the car
   def __init__(self,brand,color): # initialising the attributes
      self.brand = brand
      self.color = color


def start_engine(self):
   print(f"The engine of the car has started")

   # Creating an instance and calling the method
cars = Car("Premior", "Grey")   
start_engine('self')